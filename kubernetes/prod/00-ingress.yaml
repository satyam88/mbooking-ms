apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prod-mbooking-ms-ingress
  namespace: prod
  labels:
    env: prod
    app: mbooking-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: prod-mbooking-ms.cloudcortex.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: prod-mbooking-ms-service
                port:
                  number: 8080
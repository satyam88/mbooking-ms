apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: preprod-mbooking-ms-ingress
  namespace: preprod
  labels:
    env: preprod
    app: mbooking-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: preprod-mbooking-ms.cloudcortex.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: preprod-mbooking-ms-service
                port:
                  number: 8080